<div class="auth_container">

    <div class="auth_inner"></div>
    <div class="auth_submit">
        {{#unless isLogout}}
            <button type="button" class="auth__button auth_submit__button" data-sw-translate
                {{#if isDisableLoginButton}}disabled{{/if}}>Authorize</button>
        {{/unless}}
        {{#if isAuthorized}}
            <button type="button" class="auth__button auth_logout__button" data-sw-translate>Logout</button>
        {{/if}}
    </div>

</div>
